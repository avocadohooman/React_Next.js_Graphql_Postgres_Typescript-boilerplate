mutation Register($username: String!, $password: String!) {
    register(password: $password, username: $username) {
    errors {
        field
        message
    }
    user {
        ...RegularUser
    }
}
}
